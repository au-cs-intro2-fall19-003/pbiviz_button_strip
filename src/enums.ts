export enum Align {
    center = "center",
    left = "left",
    right = "right"
}

export enum Button_Sizing_Method {
    uniform = "uniform",
    fixed = "fixed",
    dynamic = "dynamic"
}

export enum Button_Layout {
    horizontal = "horizontal",
    vertical = "vertical",
    grid = "grid"
}